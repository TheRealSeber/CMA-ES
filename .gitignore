# Local-only design/planning docs (never committed)
docs/superpowers/

# Python / uv
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# Heavy raw experiment data (per konspekt: >5 MB goes outside repo; manifest stays)
results/*.parquet
results/figures/
full_run.log

# LaTeX build artifacts
report/*.aux
report/*.log
report/*.out
report/*.toc
report/*.fls
report/*.fdb_latexmk
report/*.synctex.gz
